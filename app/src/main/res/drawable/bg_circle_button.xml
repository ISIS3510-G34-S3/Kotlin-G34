<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Relleno blanco -->
    <solid android:color="@android:color/white" />

    <!-- Borde gris suave -->
    <stroke
        android:width="1dp"
        android:color="@color/grey_300" />

    <!-- Tamaño mínimo para asegurar que se vea redondito -->
    <size
        android:width="32dp"
        android:height="32dp" />
</shape>
